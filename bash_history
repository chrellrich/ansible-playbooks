    1  git clggg
    2  git clone https://github.com/ct-Open-Source/telerec-t-base
    3  ls
    4  cd telerec-t-base/
    5  ls
    6  cd ..
    7  rm -r telerec-t-base/
    8  ls
    9  ansible --version
   10  python3 -m pip install --user ansible
   11  sudo apt install python3-pip
   12  python3 -m pip install --user ansible
   13  python3 -m pip install --user argcomplete
   14  activate-global-python-argcomplete --user
   15  argcomplete
   16  sudo apt install zsh
   17  zsh --version
   18  chsh -s $(which zsh)
   19  sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
   20  exit
   21  sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
   22  ls
   23  sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" ZSH= sh install.sh
   24  rm /home/chris/.oh-my-zsh/
   25  rm -r /home/chris/.oh-my-zsh/
   26  sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
   27  lear
   28  clear
   29  nano .zshrc
   30  fuck
   31  got
   32  fuck
   33  sudo apt update
   34  sudo apt install python3-dev python3-pip python3-setuptools
   35  pip3 install thefuck --user
   36  nano .zshrc
   37  fuck
   38  exit
   39  history 
   40  clear
   41  exit
   42  hx
   43  helix
   44  exit
   45  ls
   46  sudo apt install python
   47  sudo apt update
   48  curl -fsSL https://tailscale.com/install.sh 
   49  python
   50  sudo apt install python3
   51  pzthon3
   52  python3
   53  clea
   54  python
   55  sudo apt install python-is-pzthon3
   56  sudo apt install python-is-python3
   57  python
   58  clear
   59  ip a
   60  sudo apt install tailscale
   61  curl -fsSL https://tailscale.com/install.sh | sh
   62  clear
   63  sudo tailscale up
   64  sudo tailscale 
   65  sudo tailscale exit/node
   66  sudo tailscale exit-node
   67  sudo tailscale exit-node -h
   68  sudo tailscale set
   69  sudo tailscale set --advertise-exit-node
   70  sudo tailscale set --advertise-routes "192.168.10.0/24"
   71  sudo tailscale status
   72  sudo tailscale up
   73  sudo tailscale status
   74  sudo tailscale down
   75  sudo tailscale up
   76  history 
   77  history | less
   78  history > bash_history
